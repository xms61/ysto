#!/bin/sh
# Blocks commits that would publish media, data, secrets or details of this machine.
# Enable once per clone: git config core.hooksPath .githooks
exec node scripts/check-tracked-files.mjs --staged
