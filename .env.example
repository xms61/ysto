# Copy to .env and uncomment what you change. Every variable the app reads is listed here with its default.
# `npm run dev` and `npm start` load .env; variables already set in the environment win.

# HTTP port of the server.
# PORT=3000
