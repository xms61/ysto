# Allowlist: the image build sees only what is listed here, so a new data folder stays out by default.
*
!package.json
!package-lock.json
!tsconfig*.json
!vite.config.ts
!index.html
!server/
!shared/
!src/
!public/
